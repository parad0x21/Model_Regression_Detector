"""Feature-agnostic core primitives shared across the platform.

This package holds only generic building blocks (ID generation, content hashing,
and—later—domain models and the feature registry). It must never contain
feature-specific logic.
"""
